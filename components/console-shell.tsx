"use client";

import { FormEvent, useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import {
  Activity, ArrowUpRight, BarChart3, Bell, Check, ChevronRight,
  CircleHelp, CreditCard, KeyRound, LayoutDashboard, Loader2, LogOut, Menu,
  Plus, ReceiptText, Server, Settings2, Sparkles, X,
} from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { useAuth } from "@/components/auth-gate";
import { Account, ApiKey, Plan, Subscription, Usage, User, apiFetch, taskInputSchema } from "@/lib/api";
import { cn } from "@/lib/utils";
import { AdminConsole } from "@/components/admin-console";

type View = "overview" | "tasks" | "accounts" | "billing" | "keys" | "admin";
type Task = { id: string; name: string; account_id: string; targets: string[]; schedule_time: string; enabled: boolean; archived: boolean; next_run_at: string | null };
type Run = { id: string; task_id: string; status: string; trigger_type: string; sent_count: number; result: string | null; cancel_requested: boolean; worker_id: string | null; created_at: string; finished_at: string | null };
type AdminOrder = { id: string; plan_id: string; cycle: string; amount_cents: number; status: string; provider_trade_no: string | null; created_at: string };

const nav = [
  { id: "overview" as View, label: "概览", icon: LayoutDashboard },
  { id: "tasks" as View, label: "任务中心", icon: Activity },
  { id: "accounts" as View, label: "抖音资源", icon: Server },
  { id: "billing" as View, label: "订阅与账单", icon: CreditCard },
  { id: "keys" as View, label: "API 密钥", icon: KeyRound },
];

function ErrorText({ message }: { message: string }) {
  return message ? <p role="alert" className="border border-[#f0c4be] bg-[#fff7f5] px-3 py-2 text-xs text-[#b43c2d]">{message}</p> : null;
}

function SideNav({ active, onSelect, onClose, onProfile, user }: { active: View; onSelect: (view: View) => void; onClose?: () => void; onProfile: () => void; user: User }) {
  return <aside className="flex h-full w-[244px] shrink-0 flex-col border-r border-[#e7e7e7] bg-[#fbfbfa]">
    <div className="flex h-[72px] items-center justify-between border-b border-[#e7e7e7] px-6"><div className="flex items-center gap-2.5"><div className="flex h-7 w-7 items-center justify-center rounded-[6px] bg-[#161616] text-white"><Sparkles size={14} /></div><span className="font-semibold tracking-[-.03em]">sparkflow</span></div>{onClose && <Button variant="ghost" size="sm" aria-label="关闭菜单" onClick={onClose}><X size={16} /></Button>}</div>
    <div className="px-3 py-5"><div className="mb-2 px-3 text-[10px] font-semibold uppercase tracking-[.18em] text-[#9a9a9a]">Workspace</div>{nav.map((item) => { const Icon = item.icon; return <button key={item.id} onClick={() => { onSelect(item.id); onClose?.(); }} className={cn("flex h-10 w-full items-center gap-3 rounded-[5px] px-3 text-sm transition", active === item.id ? "bg-[#161616] text-white" : "text-[#707070] hover:bg-[#f0f0ee] hover:text-[#161616]")}><Icon size={16} strokeWidth={1.8} /><span>{item.label}</span>{item.id === "billing" && <Badge tone="orange" className="ml-auto border-0 bg-[#f6e7d0] text-[10px]">PRO</Badge>}</button>; })}{user.role === "ADMIN" && <><div className="mb-2 mt-8 px-3 text-[10px] font-semibold uppercase tracking-[.18em] text-[#9a9a9a]">System</div><button onClick={() => { onSelect("admin"); onClose?.(); }} className={cn("flex h-10 w-full items-center gap-3 rounded-[5px] px-3 text-sm transition", active === "admin" ? "bg-[#161616] text-white" : "text-[#707070] hover:bg-[#f0f0ee] hover:text-[#161616]")}><BarChart3 size={16} strokeWidth={1.8} /><span>管理控制台</span></button></>}</div>
    <div className="mt-auto border-t border-[#e7e7e7] p-4"><button onClick={onProfile} className="flex w-full items-center gap-3 rounded-[6px] p-2 text-left hover:bg-[#f0f0ee]"><div className="flex h-8 w-8 items-center justify-center rounded-full bg-[#e8e8e6] text-xs font-semibold">{user.name.slice(0, 2).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-xs font-medium">{user.name}</p><p className="truncate text-[11px] text-[#909090]">{user.email}</p></div><Settings2 size={15} className="text-[#909090]" /></button></div>
  </aside>;
}

function Topbar({ title, onMenu, onLogout }: { title: string; onMenu: () => void; onLogout: () => void }) {
  return <header className="flex h-[72px] items-center justify-between border-b border-[#e7e7e7] bg-[#fbfbfa] px-5 sm:px-8"><div className="flex items-center gap-3"><Button variant="ghost" size="sm" className="md:hidden" aria-label="打开菜单" onClick={onMenu}><Menu size={18} /></Button><div><div className="text-[11px] text-[#9a9a9a]">Workspace / Console</div><h1 className="text-[17px] font-semibold tracking-[-.03em]">{title}</h1></div></div><div className="flex items-center gap-2"><Button variant="ghost" size="sm" aria-label="帮助"><CircleHelp size={17} /></Button><Button variant="ghost" size="sm" aria-label="通知"><Bell size={17} /></Button><div className="ml-2 h-6 w-px bg-[#e7e7e7]" /><Button variant="ghost" size="sm" aria-label="退出登录" onClick={onLogout}><LogOut size={16} /></Button></div></header>;
}

function StatCard({ label, value, hint, mark }: { label: string; value: string; hint: string; mark?: string }) {
  return <Card className="p-5"><div className="flex items-center justify-between text-xs text-[#858585]"><span>{label}</span>{mark && <span className="text-[#287b42]">{mark}</span>}</div><div className="mt-4 text-[26px] font-semibold tracking-[-.05em]">{value}</div><div className="mt-1 text-xs text-[#909090]">{hint}</div></Card>;
}

function Overview({ onSelect, user }: { onSelect: (view: View) => void; user: User }) {
  const [usage, setUsage] = useState<Usage | null>(null); const [subscription, setSubscription] = useState<Subscription | null>(null); const [runs, setRuns] = useState<Run[]>([]); const [error, setError] = useState("");
  useEffect(() => { Promise.all([apiFetch<Usage>("/usage"), apiFetch<Subscription | null>("/subscription"), apiFetch<Run[]>("/runs")]).then(([nextUsage, nextSubscription, nextRuns]) => { setUsage(nextUsage); setSubscription(nextSubscription); setRuns(nextRuns); }).catch((reason) => setError(reason instanceof Error ? reason.message : "概览加载失败")); }, []);
  const expires = subscription?.expires_at ? new Date(subscription.expires_at).toLocaleDateString("zh-CN") : "未订阅";
  return <div className="space-y-6"><ErrorText message={error} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="本月已发送" value={usage ? usage.used.toLocaleString() : "—"} hint={usage ? `剩余 ${usage.remaining.toLocaleString()} 次` : "等待用量数据"} mark="↗" /><StatCard label="最近运行" value={runs.length.toLocaleString()} hint={runs[0] ? `${runs[0].status} · ${new Date(runs[0].created_at).toLocaleDateString("zh-CN")}` : "暂无运行记录"} /><StatCard label="订阅有效期" value={expires} hint={subscription ? `计划 ${String(subscription.snapshot.name ?? "已激活")}` : "前往订阅与账单"} mark="→" /><StatCard label="当前账户" value={user.role} hint={user.email} /></div><div className="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-[#e7e7e7] px-5 py-4"><div><h2 className="text-sm font-semibold">最近运行</h2><p className="mt-1 text-xs text-[#909090]">执行记录由 Python Worker 写入</p></div><Button variant="ghost" size="sm" onClick={() => onSelect("tasks")}>查看任务 <ArrowUpRight size={14} /></Button></div><div className="divide-y divide-[#eeeeec]">{runs.slice(0, 5).map((run) => <div key={run.id} className="flex items-center gap-3 px-5 py-4 text-xs"><span className={cn("h-1.5 w-1.5 rounded-full", run.status === "SUCCEEDED" ? "bg-[#3eaa5d]" : run.status === "FAILED" ? "bg-[#c74b3b]" : "bg-[#e29b32]")} /><span className="min-w-0 flex-1 truncate font-mono text-[11px]">{run.id.slice(0, 12)}</span><span className="text-[#777]">{run.status}</span><span className="text-[#999]">{run.sent_count} 次</span></div>)}{runs.length === 0 && <div className="px-5 py-10 text-center text-xs text-[#999]">还没有运行记录</div>}</div></Card><Card><div className="border-b border-[#e7e7e7] px-5 py-4"><h2 className="text-sm font-semibold">当前计划</h2><p className="mt-1 text-xs text-[#909090]">{subscription ? String(subscription.snapshot.name ?? "已订阅") : "尚未开通计划"}</p></div><div className="p-5">{subscription ? <><div className="flex items-end justify-between"><div><span className="text-[26px] font-semibold tracking-[-.06em]">{expires}</span><span className="ml-1 text-xs text-[#909090]">到期</span></div><Badge tone="green">生效中</Badge></div><div className="mt-6 space-y-3 border-t border-[#eeeeec] pt-4">{(Array.isArray(subscription.snapshot.features) ? subscription.snapshot.features : []).slice(0, 4).map((item) => <div key={String(item)} className="flex items-center gap-2 text-xs text-[#5c5c5c]"><Check size={14} className="text-[#287b42]" />{String(item)}</div>)}</div></> : <p className="py-5 text-sm text-[#777]">选择一个计划后即可创建资源与任务。</p>}<Button className="mt-6 w-full" variant="outline" onClick={() => onSelect("billing")}>{subscription ? "管理订阅" : "查看套餐"} <ChevronRight size={14} /></Button></div></Card></div><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-[#e7e7e7] px-5 py-4"><div><h2 className="text-sm font-semibold">任务入口</h2><p className="mt-1 text-xs text-[#909090]">创建、调度并监控自动化任务</p></div><Button size="sm" onClick={() => onSelect("tasks")}><Plus size={14} /> 新建任务</Button></div><div className="px-5 py-8 text-sm text-[#777]">将抖音资源接入后，任务会由独立 Worker 领取执行。</div></Card></div>;
}

function RunHistory({ refreshKey }: { refreshKey: number }) {
  const [runs, setRuns] = useState<Run[]>([]);
  const [error, setError] = useState("");
  useEffect(() => {
    let active = true;
    async function load() {
      try { const next = await apiFetch<Run[]>("/runs"); if (active) setRuns(next); }
      catch (reason) { if (active) setError(reason instanceof Error ? reason.message : "运行记录加载失败"); }
    }
    void load();
    const timer = window.setInterval(() => void load(), 4000);
    return () => { active = false; window.clearInterval(timer); };
  }, [refreshKey]);
  return <Card className="overflow-hidden"><div className="border-b border-[#e7e7e7] px-5 py-4"><h2 className="text-sm font-semibold">执行记录</h2><p className="mt-1 text-xs text-[#909090]">每 4 秒自动刷新，Worker 写入状态与结果。</p></div><ErrorText message={error} /><div className="divide-y divide-[#eeeeec]">{runs.slice(0, 20).map((run) => <div key={run.id} className="flex flex-wrap items-center gap-3 px-5 py-4 text-xs"><span className={cn("h-1.5 w-1.5 rounded-full", run.status === "SUCCEEDED" ? "bg-[#3eaa5d]" : run.status === "FAILED" ? "bg-[#c74b3b]" : "bg-[#e29b32]")} /><span className="min-w-[110px] font-mono text-[11px] text-[#777]">{run.id.slice(0, 12)}</span><Badge tone={run.status === "SUCCEEDED" ? "green" : run.status === "FAILED" ? "orange" : "neutral"}>{run.status}</Badge><span className="text-[#777]">{run.sent_count} 次发送</span><span className="min-w-0 flex-1 text-[#666]">{run.result ?? "执行中，等待 Worker 返回结果"}</span><span className="text-[11px] text-[#999]">{new Date(run.created_at).toLocaleString("zh-CN")}</span></div>)}{runs.length === 0 && <div className="px-5 py-8 text-center text-xs text-[#999]">暂无执行记录</div>}</div></Card>;
}

function TaskTable({ refreshKey, onRunQueued }: { refreshKey?: number; onRunQueued?: () => void }) {
  const [items, setItems] = useState<Task[]>([]); const [error, setError] = useState(""); const [busy, setBusy] = useState("");
  useEffect(() => { apiFetch<Task[]>("/tasks").then(setItems).catch((reason) => setError(reason instanceof Error ? reason.message : "任务加载失败")); }, [refreshKey]);
  async function run(id: string) { setBusy(id); setError(""); try { await apiFetch(`/tasks/${id}/run`, { method: "POST" }); onRunQueued?.(); } catch (reason) { setError(reason instanceof Error ? reason.message : "无法启动任务"); } finally { setBusy(""); } }
  async function archive(id: string) { setBusy(id); setError(""); try { await apiFetch(`/tasks/${id}`, { method: "DELETE" }); setItems((current) => current.filter((item) => item.id !== id)); } catch (reason) { setError(reason instanceof Error ? reason.message : "无法归档任务"); } finally { setBusy(""); } }
  return <div className="space-y-6"><div className="overflow-x-auto"><div className="px-5 pt-3"><ErrorText message={error} /></div><table className="w-full min-w-[700px] text-left text-xs"><thead className="bg-[#fafaf9] text-[10px] uppercase tracking-[.14em] text-[#989898]"><tr><th className="px-5 py-3 font-medium">任务</th><th className="px-5 py-3 font-medium">计划</th><th className="px-5 py-3 font-medium">目标</th><th className="px-5 py-3 font-medium">下次运行</th><th className="px-5 py-3 font-medium">状态</th><th className="px-5 py-3" /></tr></thead><tbody className="divide-y divide-[#eeeeec]">{items.map((task) => <tr key={task.id} className="hover:bg-[#fcfcfb]"><td className="px-5 py-4"><div className="font-medium">{task.name}</div><div className="mt-1 font-mono text-[11px] text-[#999]">{task.id.slice(0, 10)}</div></td><td className="px-5 py-4 text-[#666]">每天 {task.schedule_time}</td><td className="px-5 py-4 font-mono text-[11px]">{task.targets.length}</td><td className="px-5 py-4 text-[#666]">{task.next_run_at ? new Date(task.next_run_at).toLocaleString("zh-CN", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" }) : "手动触发"}</td><td className="px-5 py-4"><Badge tone={task.enabled ? "green" : "orange"}>{task.enabled ? "运行中" : "已暂停"}</Badge></td><td className="px-5 py-4 text-right"><div className="flex justify-end gap-1"><Button variant="ghost" size="sm" disabled={busy === task.id} onClick={() => void run(task.id)}>{busy === task.id ? <Loader2 size={13} className="animate-spin" /> : "运行"} <ArrowUpRight size={14} /></Button><Button variant="ghost" size="sm" aria-label={`归档${task.name}`} disabled={busy === task.id} onClick={() => void archive(task.id)}><X size={14} /></Button></div></td></tr>)}{items.length === 0 && <tr><td colSpan={6} className="px-5 py-10 text-center text-xs text-[#999]">暂无任务，请先添加抖音资源并创建任务。</td></tr>}</tbody></table></div><RunHistory refreshKey={refreshKey ?? 0} /></div>;
}

function TaskForm({ onCreated }: { onCreated: () => void }) {
  const [accounts, setAccounts] = useState<Account[]>([]); const [form, setForm] = useState({ account_id: "", name: "", targets: "", message: "今天也要记得续上火花", hitokoto_types: [] as string[], schedule_time: "21:30", timezone: "Asia/Shanghai", enabled: false }); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  useEffect(() => { apiFetch<Account[]>("/accounts").then(setAccounts).catch(() => undefined); }, []);
  async function submit(event: FormEvent) { event.preventDefault(); setError(""); const parsed = taskInputSchema.safeParse({ ...form, targets: form.targets.split(/[,，\n]/).map((item) => item.trim()).filter(Boolean) }); if (!parsed.success) { setError(parsed.error.issues[0]?.message ?? "请检查表单"); return; } if (!parsed.data.account_id) { setError("请先添加抖音资源"); return; } setBusy(true); try { await apiFetch("/tasks", { method: "POST", body: JSON.stringify(parsed.data) }); onCreated(); } catch (reason) { setError(reason instanceof Error ? reason.message : "创建失败"); } finally { setBusy(false); } }
  return <form onSubmit={submit} className="space-y-4 border-t border-[#e7e7e7] bg-[#fcfcfb] p-5"><div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-xs font-medium">任务名称</span><input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] bg-white px-3 text-sm outline-none focus:border-[#161616]" placeholder="例如：晚间火花维护" /></label><label className="block"><span className="mb-2 block text-xs font-medium">执行资源</span><select required value={form.account_id} onChange={(event) => setForm({ ...form, account_id: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] bg-white px-3 text-sm outline-none focus:border-[#161616]"><option value="">选择抖音账号</option>{accounts.map((account) => <option value={account.id} key={account.id}>{account.name} · @{account.unique_id}</option>)}</select></label></div><label className="block"><span className="mb-2 block text-xs font-medium">目标好友</span><textarea required value={form.targets} onChange={(event) => setForm({ ...form, targets: event.target.value })} className="min-h-[72px] w-full rounded-[5px] border border-[#dcdcd9] bg-white px-3 py-2 text-sm outline-none focus:border-[#161616]" placeholder="每行或逗号分隔" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-xs font-medium">消息模板</span><input required value={form.message} onChange={(event) => setForm({ ...form, message: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] bg-white px-3 text-sm outline-none focus:border-[#161616]" /></label><label className="block"><span className="mb-2 block text-xs font-medium">每日运行时间</span><input required type="time" value={form.schedule_time} onChange={(event) => setForm({ ...form, schedule_time: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] bg-white px-3 text-sm outline-none focus:border-[#161616]" /></label></div><ErrorText message={error} /><div className="flex justify-end"><Button type="submit" disabled={busy}>{busy ? <Loader2 size={14} className="animate-spin" /> : <Plus size={14} />}创建任务</Button></div></form>;
}

function Accounts() {
  const [accounts, setAccounts] = useState<Account[]>([]); const [form, setForm] = useState({ name: "", unique_id: "", cookies: "" }); const [open, setOpen] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState("");
  async function load() { try { setAccounts(await apiFetch<Account[]>("/accounts")); } catch (reason) { setError(reason instanceof Error ? reason.message : "资源加载失败"); } }
  useEffect(() => { apiFetch<Account[]>("/accounts").then(setAccounts).catch((reason) => setError(reason instanceof Error ? reason.message : "资源加载失败")); }, []);
  async function submit(event: FormEvent) { event.preventDefault(); setError(""); let cookies: unknown; try { cookies = JSON.parse(form.cookies); if (!Array.isArray(cookies)) throw new Error(); } catch { setError("Cookie 必须是 JSON 数组"); return; } setBusy(true); try { await apiFetch("/accounts", { method: "POST", body: JSON.stringify({ name: form.name, unique_id: form.unique_id, cookies }) }); setForm({ name: "", unique_id: "", cookies: "" }); setOpen(false); await load(); } catch (reason) { setError(reason instanceof Error ? reason.message : "资源创建失败"); } finally { setBusy(false); } }
  async function remove(id: string) { try { await apiFetch(`/accounts/${id}`, { method: "DELETE" }); setAccounts((current) => current.filter((account) => account.id !== id)); } catch (reason) { setError(reason instanceof Error ? reason.message : "无法删除资源"); } }
  return <div className="space-y-6"><div className="flex items-end justify-between"><div><h2 className="text-sm font-semibold">抖音资源</h2><p className="mt-1 text-xs text-[#909090]">Cookie 加密存储，只在 Runner 执行时解密。</p></div><Button onClick={() => setOpen((value) => !value)}><Plus size={14} />{open ? "收起表单" : "添加账号"}</Button></div><ErrorText message={error} />{open && <Card><form onSubmit={submit} className="grid gap-4 p-5 sm:grid-cols-2"><label className="block"><span className="mb-2 block text-xs font-medium">资源名称</span><input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] px-3 text-sm outline-none focus:border-[#161616]" placeholder="主账号" /></label><label className="block"><span className="mb-2 block text-xs font-medium">抖音号 / unique_id</span><input required value={form.unique_id} onChange={(event) => setForm({ ...form, unique_id: event.target.value })} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] px-3 text-sm outline-none focus:border-[#161616]" /></label><label className="block sm:col-span-2"><span className="mb-2 block text-xs font-medium">Cookie JSON</span><textarea required value={form.cookies} onChange={(event) => setForm({ ...form, cookies: event.target.value })} className="min-h-[110px] w-full rounded-[5px] border border-[#dcdcd9] px-3 py-2 font-mono text-xs outline-none focus:border-[#161616]" placeholder='[{"name":"sessionid","value":"...","domain":".douyin.com","path":"/"}]' /></label><div className="flex justify-end sm:col-span-2"><Button type="submit" disabled={busy}>{busy ? "保存中" : "保存资源"}</Button></div></form></Card>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{accounts.map((account) => <Card key={account.id} className="p-5"><div className="flex items-start justify-between"><div className="flex h-10 w-10 items-center justify-center rounded-[6px] bg-[#e9f5eb] text-sm font-semibold">{account.name.slice(0, 1)}</div><Badge tone={account.status === "READY" ? "green" : "orange"}>{account.status === "READY" ? "可用" : account.status}</Badge></div><h3 className="mt-5 text-sm font-semibold">{account.name}</h3><p className="mt-1 font-mono text-[11px] text-[#888]">@{account.unique_id}</p><div className="mt-5 flex items-center justify-between border-t border-[#eeeeec] pt-4 text-xs text-[#777]"><span>{new Date(account.updated_at).toLocaleDateString("zh-CN")}</span><Button variant="ghost" size="sm" aria-label={`删除${account.name}`} onClick={() => void remove(account.id)}><X size={14} /></Button></div></Card>)}{accounts.length === 0 && <Card className="p-8 text-center text-xs text-[#999] md:col-span-2 xl:col-span-3">暂无资源，请添加一个你有权管理的账号。</Card>}</div></div>;
}

function Billing() {
  const [plans, setPlans] = useState<Plan[]>([]); const [subscription, setSubscription] = useState<Subscription | null>(null); const [orders, setOrders] = useState<AdminOrder[]>([]); const [cycle, setCycle] = useState<"monthly" | "quarterly" | "yearly">("monthly"); const [error, setError] = useState(""); const [busy, setBusy] = useState(""); const checkoutSequence = useRef(0);
  useEffect(() => { Promise.all([apiFetch<Plan[]>("/plans"), apiFetch<Subscription | null>("/subscription"), apiFetch<AdminOrder[]>("/billing/orders")]).then(([nextPlans, nextSubscription, nextOrders]) => { setPlans(nextPlans); setSubscription(nextSubscription); setOrders(nextOrders); }).catch((reason) => setError(reason instanceof Error ? reason.message : "账单加载失败")); }, []);
  async function checkout(plan: Plan) { setBusy(plan.id); setError(""); checkoutSequence.current += 1; try { const order = await apiFetch<{ checkout: { url?: string } | null }>("/billing/checkout", { method: "POST", body: JSON.stringify({ plan_id: plan.id, cycle, payment_method: "alipay", idempotency_key: `${plan.id}-${cycle}-${checkoutSequence.current}` }) }); if (order.checkout?.url) window.location.assign(order.checkout.url); else setError("支付网关尚未配置，请联系管理员"); } catch (reason) { setError(reason instanceof Error ? reason.message : "创建订单失败"); } finally { setBusy(""); } }
  return <div className="space-y-6"><ErrorText message={error} /><Card className="flex flex-col justify-between gap-5 p-6 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2"><span className="text-sm font-semibold">{subscription ? String(subscription.snapshot.name ?? "已订阅") : "未订阅"}</span>{subscription && <Badge tone="green">生效中</Badge>}</div><p className="mt-2 text-xs text-[#777]">{subscription?.expires_at ? `到期：${new Date(subscription.expires_at).toLocaleDateString("zh-CN")}` : "开通计划后解锁任务与资源配额"}</p></div><Button variant="outline" onClick={() => document.getElementById("orders")?.scrollIntoView({ behavior: "smooth" })}>查看订单记录 <ReceiptText size={14} /></Button></Card><div><div className="mb-4 flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-sm font-semibold">选择一个计划</h2><p className="mt-1 text-xs text-[#909090]">支持月付、季付和年付，支付成功后自动激活。</p></div><div className="flex rounded-[5px] border border-[#dcdcd9] bg-white p-1 text-xs">{(["monthly", "quarterly", "yearly"] as const).map((value) => <button key={value} onClick={() => setCycle(value)} className={cn("rounded-[3px] px-3 py-1.5", cycle === value ? "bg-[#161616] text-white" : "text-[#777]")}>{value === "monthly" ? "月付" : value === "quarterly" ? "季付" : "年付"}</button>)}</div></div><div className="grid gap-4 lg:grid-cols-3">{plans.map((plan) => { const cents = plan[`${cycle}_cents`]; const current = subscription?.plan_id === plan.id; return <Card key={plan.id} className={cn("relative p-5", current && "border-[#161616]")}><div className="flex items-start justify-between"><div><h3 className="font-semibold">{plan.name}</h3><p className="mt-1 text-xs text-[#888]">{plan.description}</p></div>{current && <Badge>当前计划</Badge>}</div><div className="mt-6"><span className="text-3xl font-semibold tracking-[-.06em]">¥{(cents / 100).toFixed(0)}</span><span className="text-xs text-[#888]"> / {cycle === "monthly" ? "月" : cycle === "quarterly" ? "季" : "年"}</span></div><div className="my-5 space-y-3 border-y border-[#eeeeec] py-5">{plan.features.map((feature) => <div key={feature} className="flex items-center gap-2 text-xs text-[#555]"><Check size={14} className="text-[#287b42]" />{feature}</div>)}</div><Button className="w-full" variant={current ? "outline" : "solid"} disabled={current || busy === plan.id} onClick={() => void checkout(plan)}>{busy === plan.id ? <Loader2 size={14} className="animate-spin" /> : current ? "当前使用中" : "开通计划"}</Button></Card>; })}</div></div><Card id="orders" className="overflow-hidden"><div className="border-b border-[#e7e7e7] px-5 py-4"><h2 className="text-sm font-semibold">订单记录</h2></div><div className="divide-y divide-[#eeeeec]">{orders.map((order) => <div key={order.id} className="flex flex-wrap items-center gap-3 px-5 py-4 text-xs"><span className="font-mono text-[11px] text-[#888]">{order.id.slice(0, 12)}</span><span className="text-[#777]">{order.cycle}</span><span className="font-mono">¥{(order.amount_cents / 100).toFixed(2)}</span><Badge tone={order.status === "PAID" ? "green" : order.status === "PENDING" ? "orange" : "neutral"}>{order.status}</Badge><span className="ml-auto text-[11px] text-[#999]">{new Date(order.created_at).toLocaleDateString("zh-CN")}</span></div>)}{orders.length === 0 && <div className="px-5 py-8 text-center text-xs text-[#999]">暂无订单</div>}</div></Card></div>;
}

function Keys() {
  const [keys, setKeys] = useState<ApiKey[]>([]); const [name, setName] = useState(""); const [token, setToken] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false);
  async function load() { try { setKeys(await apiFetch<ApiKey[]>("/api-keys")); } catch (reason) { setError(reason instanceof Error ? reason.message : "密钥加载失败"); } }
  useEffect(() => { apiFetch<ApiKey[]>("/api-keys").then(setKeys).catch((reason) => setError(reason instanceof Error ? reason.message : "密钥加载失败")); }, []);
  async function create(event: FormEvent) { event.preventDefault(); setBusy(true); setError(""); try { const created = await apiFetch<ApiKey>("/api-keys", { method: "POST", body: JSON.stringify({ name, expires_days: 90 }) }); setToken(created.token ?? ""); setName(""); await load(); } catch (reason) { setError(reason instanceof Error ? reason.message : "密钥创建失败"); } finally { setBusy(false); } }
  async function revoke(id: string) { try { await apiFetch(`/api-keys/${id}`, { method: "DELETE" }); await load(); } catch (reason) { setError(reason instanceof Error ? reason.message : "密钥撤销失败"); } }
  return <div className="space-y-6"><ErrorText message={error} />{token && <Card className="border-[#cde5d2] bg-[#f4fbf5] p-5"><p className="text-xs font-medium text-[#287b42]">新密钥只显示这一次</p><code className="mt-3 block break-all rounded-[4px] bg-white p-3 font-mono text-xs">{token}</code></Card>}<Card className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-sm font-semibold">个人 API 密钥</h2><p className="mt-1 max-w-xl text-xs leading-5 text-[#777]">密钥只存储哈希值，用于受控的自动化调用。</p></div><form onSubmit={create} className="flex w-full gap-2 sm:w-auto"><input required value={name} onChange={(event) => setName(event.target.value)} className="h-9 min-w-0 rounded-[5px] border border-[#dcdcd9] px-3 text-xs outline-none focus:border-[#161616]" placeholder="密钥名称" /><Button type="submit" disabled={busy}><Plus size={14} />创建</Button></form></Card><Card className="overflow-hidden"><div className="border-b border-[#e7e7e7] px-5 py-4"><h2 className="text-sm font-semibold">已创建的密钥</h2></div><div className="divide-y divide-[#eeeeec]">{keys.map((key) => <div className="flex flex-col gap-3 px-5 py-4 sm:flex-row sm:items-center" key={key.id}><KeyRound size={16} className="text-[#888]" /><div className="min-w-0 flex-1"><div className="text-xs font-medium">{key.name}</div><div className="mt-1 font-mono text-[11px] text-[#888]">{key.prefix}••••</div></div><span className="text-[11px] text-[#999]">到期 {new Date(key.expires_at).toLocaleDateString("zh-CN")}</span><Badge tone={key.revoked_at ? "neutral" : "green"}>{key.revoked_at ? "已撤销" : "有效"}</Badge>{!key.revoked_at && <Button variant="ghost" size="sm" aria-label={`撤销${key.name}`} onClick={() => void revoke(key.id)}><X size={14} /></Button>}</div>)}{keys.length === 0 && <div className="px-5 py-8 text-center text-xs text-[#999]">暂无 API 密钥</div>}</div></Card></div>;
}

function Admin() {
  return <AdminConsole />;
}

function ProfileDialog({ user, onClose, onUpdated }: { user: User; onClose: () => void; onUpdated: () => Promise<void> }) {
  const [name, setName] = useState(user.name);
  const [currentPassword, setCurrentPassword] = useState("");
  const [newPassword, setNewPassword] = useState("");
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [busy, setBusy] = useState(false);
  async function saveProfile(event: FormEvent) {
    event.preventDefault();
    setBusy(true); setError(""); setMessage("");
    try {
      await apiFetch("/me", { method: "PATCH", body: JSON.stringify({ name }) });
      await onUpdated();
      setMessage("资料已更新，重新打开后会显示新名称。");
    } catch (reason) {
      setError(reason instanceof Error ? reason.message : "资料更新失败");
    } finally { setBusy(false); }
  }
  async function savePassword(event: FormEvent) {
    event.preventDefault();
    setBusy(true); setError(""); setMessage("");
    try {
      await apiFetch("/me/password", { method: "PATCH", body: JSON.stringify({ current_password: currentPassword, new_password: newPassword }) });
      setCurrentPassword(""); setNewPassword(""); setMessage("密码已更新，其他登录会话已失效。");
    } catch (reason) {
      setError(reason instanceof Error ? reason.message : "密码更新失败");
    } finally { setBusy(false); }
  }
  return <div className="fixed inset-0 z-50 grid place-items-center bg-black/20 px-5"><Card className="w-full max-w-[520px] p-6 shadow-xl"><div className="flex items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[.18em] text-[#999]">Profile</p><h2 className="mt-2 text-lg font-semibold">个人中心</h2><p className="mt-1 text-xs text-[#777]">{user.email}</p></div><Button variant="ghost" size="sm" aria-label="关闭个人中心" onClick={onClose}><X size={16} /></Button></div><div className="mt-6 grid gap-6 sm:grid-cols-2"><form onSubmit={saveProfile} className="space-y-3"><h3 className="text-xs font-semibold">基本资料</h3><input required minLength={2} value={name} onChange={(event) => setName(event.target.value)} className="h-10 w-full rounded-[5px] border border-[#dcdcd9] px-3 text-sm outline-none focus:border-[#161616]" /><Button type="submit" disabled={busy} size="sm">保存资料</Button></form><form onSubmit={savePassword} className="space-y-3"><h3 className="text-xs font-semibold">修改密码</h3><input required type="password" autoComplete="current-password" value={currentPassword} onChange={(event) => setCurrentPassword(event.target.value)} placeholder="当前密码" className="h-10 w-full rounded-[5px] border border-[#dcdcd9] px-3 text-xs outline-none focus:border-[#161616]" /><input required minLength={8} type="password" autoComplete="new-password" value={newPassword} onChange={(event) => setNewPassword(event.target.value)} placeholder="新密码（至少 8 位）" className="h-10 w-full rounded-[5px] border border-[#dcdcd9] px-3 text-xs outline-none focus:border-[#161616]" /><Button type="submit" disabled={busy} variant="outline" size="sm">更新密码</Button></form></div>{(message || error) && <div className="mt-5"><p className={cn("text-xs", error ? "text-[#b43c2d]" : "text-[#287b42]")}>{error || message}</p></div>}</Card></div>;
}

export function ConsoleShell() {
  const user = useAuth(); const router = useRouter(); const [active, setActive] = useState<View>("overview"); const [mobileOpen, setMobileOpen] = useState(false); const [newTask, setNewTask] = useState(false); const [taskRefresh, setTaskRefresh] = useState(0); const [profileOpen, setProfileOpen] = useState(false); const titles: Record<View, string> = { overview: "概览", tasks: "任务中心", accounts: "抖音资源", billing: "订阅与账单", keys: "API 密钥", admin: "管理控制台" };
  if (!user) return null;
  async function logout() { await apiFetch("/auth/logout", { method: "POST" }).catch(() => undefined); router.push("/login"); }
  return <div className="flex min-h-screen"><div className="hidden md:block"><SideNav active={active} onSelect={setActive} onProfile={() => setProfileOpen(true)} user={user} /></div>{mobileOpen && <div className="fixed inset-0 z-40 flex md:hidden"><button className="absolute inset-0 bg-black/20" aria-label="关闭菜单" onClick={() => setMobileOpen(false)} /><div className="relative z-10 h-full"><SideNav active={active} onSelect={setActive} onClose={() => setMobileOpen(false)} onProfile={() => { setProfileOpen(true); setMobileOpen(false); }} user={user} /></div></div>}<main className="min-w-0 flex-1"><Topbar title={titles[active]} onMenu={() => setMobileOpen(true)} onLogout={() => void logout()} /><div className="grid-paper min-h-[calc(100vh-72px)] p-5 sm:p-8"><div className="mx-auto max-w-[1280px]">{active === "overview" && <Overview onSelect={setActive} user={user} />}{active === "tasks" && <div className="space-y-6"><div className="flex items-end justify-between"><div><h2 className="text-sm font-semibold">任务中心</h2><p className="mt-1 text-xs text-[#909090]">创建、调度并监控你的自动化任务。</p></div><Button onClick={() => setNewTask((value) => !value)}><Plus size={14} />{newTask ? "收起表单" : "新建任务"}</Button></div>{newTask && <Card><TaskForm onCreated={() => { setNewTask(false); setTaskRefresh((value) => value + 1); }} /></Card>}<Card><TaskTable refreshKey={taskRefresh} onRunQueued={() => setTaskRefresh((value) => value + 1)} /></Card></div>}{active === "accounts" && <Accounts />}{active === "billing" && <Billing />}{active === "keys" && <Keys />}{active === "admin" && user.role === "ADMIN" && <Admin />}{profileOpen && <ProfileDialog user={user} onClose={() => setProfileOpen(false)} onUpdated={user.refreshUser} />}</div></div></main></div>;
}
